# Production EKS overrides (merge with values.yaml)
replicaCount: 2

ingress:
  host: api.saloon.example.com

env:
  CORS_ALLOWED_ORIGINS: "https://saloon.example.com,https://api.saloon.example.com"
  MANAGEMENT_TRACING_SAMPLING_PROBABILITY: "0.25"
