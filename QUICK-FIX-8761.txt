================================================================================
QUICK FIX FOR PORT 8761 ERROR
================================================================================

Since you're getting the error RIGHT NOW, here's what to do:

STEP 1: Open PowerShell as Administrator
   - Press Windows Key + X
   - Select "Windows PowerShell (Admin)" or "Terminal (Admin)"

STEP 2: Copy and paste this EXACT command:

Get-NetTCPConnection -LocalPort 8761 -ErrorAction SilentlyContinue | ForEach-Object { Write-Host "Killing PID: $($_.OwningProcess)"; Stop-Process -Id $_.OwningProcess -Force }

STEP 3: Press Enter

STEP 4: If that doesn't work, try this:

$pids = @(); netstat -ano | Select-String ":8761" | ForEach-Object { $parts = $_ -split '\s+'; $pid = $parts[-1]; if ($pid -match '^\d+$') { $pids += [int]$pid } }; $pids | Select-Object -Unique | ForEach-Object { Write-Host "Killing PID: $_"; taskkill /PID $_ /F }

STEP 5: Verify it's free:

netstat -ano | findstr :8761

If nothing shows up, the port is FREE!

================================================================================
ALTERNATIVE: Use the batch files I created
================================================================================

1. Right-click "KILL-8761.bat" 
2. Select "Run as Administrator"
3. Follow the prompts

================================================================================
LAST RESORT: Kill all Java processes
================================================================================

WARNING: This will close ALL Java applications (IDEs, browsers, etc.)

Get-Process java* -ErrorAction SilentlyContinue | Stop-Process -Force

================================================================================

